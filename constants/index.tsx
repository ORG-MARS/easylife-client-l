import Colors from './Colors'
import Layout from './Layout'


const Domain = 'www.yuedingyixia.com'
const API = `https://${Domain}/api`
const WebSocketDomain = `wss://${Domain}/ws`
export {
    Colors,
    Layout,
    Domain,
    API,
    WebSocketDomain
}
